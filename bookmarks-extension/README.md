# Browser extension (placeholder)

Companion extension for **bookmarks-sync** will live here.

Planned:

- Sync bookmarks to/from the self-hosted API
- API base URL + API key configuration
- Manual sync + optional interval sync

The server API is ready; see the root `README.md` for endpoints.
