node_modules/
data/*.db
data/*.db-*
data/*.db.bak*
data/logs/
.env
*.log
.DS_Store
# Built packages (unsigned rebuilds, intermediate artifacts)
dist/*
# Keep release packages in the repo
!dist/
!dist/*.xpi
!dist/*.zip
# web-ext / AMO local artifacts (never commit)
**/web-ext-artifacts/
**/.amo-upload-uuid

